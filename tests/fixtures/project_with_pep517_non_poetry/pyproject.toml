[build-system]
requires = ["flit_core >=3.7.1,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "flit"
authors = []
dependencies = [
    "flit_core >=3.7.1",
]
requires-python = ">=3.6"
readme = "README.rst"
dynamic = ['version', 'description']
