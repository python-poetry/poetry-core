[project]
name = "with_bad_path_dep"
version = "1.2.3"
description = "Some description."
authors = [
    { name = "Awesome Hacker", email = "awesome@hacker.io" }
]
python-requires = ">=3.6,<4.0"
dependencies = [
    "dev",
]

[tool.poetry.dependencies]
bogus = { path = "../only/in/dev", develop = true }
